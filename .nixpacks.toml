# Force Nixpacks to use Python ONLY - Skip Node.js entirely
[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["pip install -r backend/models/requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "cd backend/models && python pytorch_service.py"

